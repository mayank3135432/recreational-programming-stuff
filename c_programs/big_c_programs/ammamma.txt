let person A;
let person B;
def Act(x):
    if(x is active)
        return 1; //true
    else
        return 0; //false
def P(x):
    if(x has health issue)
        return 1; //true
    else
        return 0; //false


if(( NOT Act(A) implies P(A) ) AND ( Act(B) implies P(B) ))
    A=Ammamma;
    B=Thatendi
    print(A is Ammamma);
    print(B is Thatendi);
else
    print(A,B are not Ammamma and Thatendi)



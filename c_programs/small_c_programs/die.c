#include<stdio.h>
int main(){
    int a,b,c;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    int max,min,mid;
    if(a>b){
        if(a>c){
            max=a;
            if(c>b) mi
        }
        else max=c;
    }
    else{
        if(b>c) max=b;
        else max=c;
    }
}